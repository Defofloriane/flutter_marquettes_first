import 'package:flutter/material.dart';

class ListeBlocsUp extends StatelessWidget {
  final ScrollController controller;
  const ListeBlocsUp({Key? key, required this.controller}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
